% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_seurat.R
\name{ViewSeurat}
\alias{ViewSeurat}
\alias{view_seurat}
\title{View a Seurat Object Interactively}
\usage{
ViewSeurat(
  obj = NULL,
  title = NULL,
  launch.browser = TRUE,
  port = NULL,
  host = "127.0.0.1",
  config = NULL
)

view_seurat(
  obj = NULL,
  title = NULL,
  launch.browser = TRUE,
  port = NULL,
  host = "127.0.0.1",
  config = NULL
)
}
\arguments{
\item{obj}{A Seurat object to view. If NULL, launches the app with
the upload interface visible.}

\item{title}{Character string for the application title displayed in the
dashboard header. If NULL (default), no custom title is shown. When a
file is uploaded via the interface, the filename is displayed automatically.}

\item{launch.browser}{Logical indicating whether to open the app in
a browser window. Default is TRUE.}

\item{port}{The TCP port that the application should listen on. If NULL
(the default), a random port will be chosen.}

\item{host}{The IPv4 address that the application should listen on.
Default is "127.0.0.1" (localhost only).}

\item{config}{A list of configuration options to override defaults.
See \code{config.yaml.example} for available options.}
}
\value{
This function does not return a value. It launches a Shiny
  application in the viewer or browser.
}
\description{
Launch an interactive Shiny dashboard to explore a Seurat v5 object.
When called with an object, the Upload tab is hidden and the viewer
navigates directly to the Overview tab. When called without an object,
the full upload interface is shown.
}
\examples{
\dontrun{
# View a Seurat object directly
ViewSeurat(seurat_obj)

# View with custom title
ViewSeurat(seurat_obj, title = "My Analysis")

# Launch without an object (shows upload interface)
ViewSeurat()

# Specify port
ViewSeurat(seurat_obj, port = 3838)
}

}
